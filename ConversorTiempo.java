package com.equipo3.conversor;

/**
 * Conversor para unidades de tiempo.
 * Permite realizar conversiones entre las unidades definidas en el enum
 * UnidadTiempo.
 */
public class ConversorTiempo {

  /**
   * Convierte un valor entre dos unidades de tiempo.
   *
   * @param valor el valor numérico a convertir
   * @param origen la unidad desde la cual se convierte
   * @param destino la unidad a la que se desea convertir
   * @return el valor convertido a la unidad destino
   */
  public double convertir(double valor,
                          UnidadTiempo origen,
                          UnidadTiempo destino) {
    return 0.0;
  }
}