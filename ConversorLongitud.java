package com.equipo3.conversor;

/**
 * Conversor para unidades de longitud.
 * Permite convertir entre distintas unidades definidas en el enum
 * UnidadLongitud.
 */
public class ConversorLongitud {

  /**
   * Convierte un valor dado entre dos unidades de longitud.
   *
   * @param valor el valor numérico a convertir
   * @param origen la unidad desde la cual se convierte
   * @param destino la unidad a la cual se desea convertir
   * @return el resultado de la conversión
   */
  public double convertir(double valor,
                          UnidadLongitud origen,
                          UnidadLongitud destino) {
    return 0.0;
  }
}